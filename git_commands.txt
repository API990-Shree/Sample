git init 

git config --global user.email "shreeniwas.deshpande@apisero.com"

git config --global user.name "Shreeniwas Deshpande"

git add <file name>

git status

git commit -m "message"

git tag --a v1.0 -m "first tag, second commit"

git log

git rm --cached <file name>
-> removes file from local repo
git rm -f <file name>
-> Removes file from staging area, working and local repo
git rm <file name>
-> Removes file from working directory








